"use strict";
const printmyData = (data) => {
    if (data.occupancyDetails.isWorking) {
        data.occupancyDetails.jobName = "das";
    }
    else {
        data.occupancyDetails.isWorking = false;
    }
};
const printmyData2 = (data) => {
    if (typeof data.age === "string") {
        data.work;
        // data.data.extensions;
    }
    else {
        // data.data.max;
    }
};
